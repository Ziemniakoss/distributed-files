spring:
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/distributed
    username: postgres
    password: postgres
  servlet:
    multipart:
      max-file-size: 1GB
server:
  port: 9000
